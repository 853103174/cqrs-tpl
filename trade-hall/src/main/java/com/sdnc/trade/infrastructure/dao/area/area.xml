<?xml version="1.0" encoding="UTF-8" ?>
<beetlsql>

    <sql id="allList">
        select * from area
        <where>
            parent_code=#{parentCode} and name like #{name}
        </where>
    </sql>

</beetlsql>
