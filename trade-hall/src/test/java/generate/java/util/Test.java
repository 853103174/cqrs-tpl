package generate.java.util;

public class Test {

	public static void main(String[] args) {
		// LocalDateTime date = LocalDateTime.of(2022, 3, 1, 0, 0, 0);
		// Duration duration = Duration.between(date, LocalDateTime.now());
		// for (long i = 0, days = duration.toDays(); i < days; ++i) {
		// LocalDateTime plusDate = date.plusDays(i);
		// String startAt = plusDate.format(DateTimeFormatter.ofPattern("yyyy-MM-dd
		// 00:00:00"));
		// String endAt = plusDate.format(DateTimeFormatter.ofPattern("yyyy-MM-dd
		// 23:59:59"));
		// System.out.println(startAt + "-->" + endAt);
		// }

		// Date now = new Date();
		// Long count = 9L;
		// if (count < 100) {
		// BigInteger remainderUserId = new BigInteger("11").remainder(new
		// BigInteger("100"));
		// String orderId = String.format("%1$ty%1$tm%1$td%2$02d%3$03d", now,
		// remainderUserId, count);
		// System.out.println(orderId);
		// } else if (count >= 100 && count < 10000) {
		// BigInteger remainderUserId = new BigInteger("11").remainder(new
		// BigInteger("10"));
		// String orderId = String.format("%1$ty%1$tm%1$td%2$d%3$04d", now,
		// remainderUserId, count);
		// System.out.println(orderId);
		// } else {
		// String orderId = String.format("%1$ty%1$tm%1$td%2$d", now, count);
		// System.out.println(orderId);
		// }

		// JavaInfo info = new JavaInfo();
		// System.out.println(ONode.stringify(info));

	}

}
